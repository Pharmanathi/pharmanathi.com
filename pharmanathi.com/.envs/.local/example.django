# General
# ------------------------------------------------------------------------------
USE_DOCKER=yes
IPYTHONDIR=/app/.ipython

# Redis
# ------------------------------------------------------------------------------
REDIS_URL=redis://redis:6379/0

# Celery
# ------------------------------------------------------------------------------

# Flower
# ------------------------------------------------------------------------------
CELERY_FLOWER_USER=debug
CELERY_FLOWER_PASSWORD=debug

# Django
# ------------------------------------------------------------------------------
DJANGO_ALLOWED_HOSTS=*,
# Add amins using the fomat names1-email1_names2-email2
ADMINS="Admin Name-Admin Email"
SERVER_EMAIL=from@rmail
DEFAULT_FROM_EMAIL=from@email
EMAIL_HOST_USER=user@email
EMAIL_HOST_PASSWORD=secret
EMAIL_HOST=host.e.mail
EMAIL_USE_TLS=True
EMAIL_PORT=587

# Social Apps
# ------------------------------------------------------------------------------
SOCIAL_APP_GOOGLE_MHP_ANDROID='provider=provider,name=name,client_id=client_id,secret=secret'
SOCIAL_APP_GOOGLE_MHP_IOS='provider=provider,name=name,client_id=client_id,secret=secret'
SOCIAL_APP_GOOGLE_PATIENT_IOS='provider=provider,name=name,client_id=client_id,secret=secret'
SOCIAL_APP_GOOGLE_PATIENT_ANDROID='provider=provider,name=name,client_id=client_id,secret=secret'


# ANYMAIL-MAILGUN
# ------------------------------------------------------------------------------
MAILGUN_API_KEY=MAILGUN_API_KEY
MAILGUN_SENDING_DOMAIN=domain.com
 # Use the default backend(django.core.mail.backends.smtp.EmailBackend) to lower cost/usage with MAILGUN
DJANGO_EMAIL_BACKEND="anymail.backends.mailgun.EmailBackend"


# PAYMENTS
# ------------------------------------------------------------------------------
PAYMENT_USER_EMAIL_FIELD="email"
PAYMENT_PAYSTACK_PROVIDER_CONFIG="secret"
